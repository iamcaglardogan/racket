import SwiftUI
import RacketCore

@main
struct RACKETApp: App {
    var body: some Scene {
        WindowGroup("RACKET") {
            // The interface is deliberately deferred to the Phase 5 checkpoint.
            EmptyView()
        }
    }
}
