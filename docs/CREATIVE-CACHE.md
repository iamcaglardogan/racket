# Creative-cache verification

Phase 4 is in progress in [draft PR 4](https://github.com/iamcaglardogan/racket/pull/4), after the accepted Phase 3 checkpoint and September 11, 2026 merge of [PR 3](https://github.com/iamcaglardogan/racket/pull/3) at `aff383d`. The current work implements headless grouping and producer guards. [CI passed at `8f94aa5`](https://github.com/iamcaglardogan/racket/actions/runs/35216679049); [TESTING.md](TESTING.md#phase-4-checks) records the evidence. No cleanup rule is enabled, and no live vendor project resolver or product interface is implemented. Vendor verification and the owner checkpoint remain open.

Only `~/Library/Caches` and `~/Library/Logs` are compiled rule roots. Candidate research has not broadened them. A directory's existence, ownership, name, or documented purpose does not verify every item within it.

## Candidate evidence

The owner confirmed that Resolve and After Effects use default cache locations. This is owner context, not verification of the configured paths; exact settings paths remain an open verification item because the proposed defaults were missing. Manual observations on September 13 and 17 used bounded metadata checks, trusted no-follow traversal, and the no-materialization policy. These were one-machine observations, separate from automated tests and production discovery. Only generic vendor paths and folder names are recorded here.

| Candidate | Evidence | Current decision and open verification |
| --- | --- | --- |
| Camera Raw: `~/Library/Caches/Adobe Camera Raw 2` | The exact directory was present and owned by the current user. Adobe lists this spelling in its [Lightroom troubleshooting guide](https://helpx.adobe.com/lightroom-classic/desktop/technical-support/performance-guidelines/lightroom-basic-troubleshooting-fix-most-issues.html). Its [Camera Raw guide](https://helpx.adobe.com/camera-raw/desktop/get-started/overview-and-setup/introduction-camera-raw.html) describes regeneration, configurable cache location, and use through multiple Adobe applications. | The only bundled candidate, `adobe.camera-raw-cache-2`, remains disabled and unverified. Treat its presentation scope as application-wide. Verify content boundaries, regeneration/recovery, and every producer identity before enabling it. |
| After Effects disk cache: `~/Library/Caches/Adobe/After Effects` | The proposed directory was missing in the bounded lookup. Adobe documents a [configurable disk cache reused across projects](https://helpx.adobe.com/after-effects/desktop/memory-storage-performance/memory-and-storage/memory-storage1.html). | No rule. A project-looking folder name does not establish attribution. The attempted preferences lookup was refused by ownership checks; that refusal was retained without changing permissions or bypassing it. |
| Adobe shared media cache | The proposed `~/Library/Caches/Adobe/Common/Media Cache` and `Media Cache Files` paths were missing. On September 17, bounded enumeration of `~/Library/Application Support/Adobe/Common` found `Media Cache` and `Media Cache Files` alongside `Motion Graphics Templates`, `Team Projects`, and `LUTs`. Adobe documents [shared media-cache databases and configurable locations](https://helpx.adobe.com/after-effects/desktop/memory-storage-performance/memory-and-storage/memory-storage1.html). | No rule or additional safe root. The mixed Application Support directory remains protected. A cache label cannot justify scanning or cleaning its siblings or parent. |
| DaVinci Resolve | The proposed `~/Movies/DaVinci Resolve` directory was missing. The installed major version is 21; the reviewed [Resolve 20 guide, p. 8](https://documents.blackmagicdesign.com/SupportNotes/DaVinci_Resolve_20_New_Features_Guide.pdf#page=8), distinguishes project-generated audio from separate proxy, cache, and gallery folders. | No rule or Movies root. Resolve 20 documentation does not verify Resolve 21 locations or behavior. Voiceover and converted audio remain protected creative output; project-specific cache configuration still needs reliable evidence. |

The [Resolve 21 guide, p. 44](https://documents.blackmagicdesign.com/SupportNotes/DaVinci_Resolve_21_New_Features_Guide.pdf#page=44), also places Speech Generator audio in the configured project media location; this supports protecting generated output but does not establish a cache root or project-cache mapping.

Bundled schema-1 version `1.1.0` gives the Camera Raw candidate a depth limit of one, a proposed age threshold of 14 days, `risk: regenerable`, and `requiresClosedApplications: true`. Both `verified` and `enabled` are false. These candidate settings do not establish removal safety. Its provisional producer list includes Lightroom Classic, Photoshop, Bridge, After Effects, and the After Effects Render Engine. It is not a complete host catalogue: Adobe's [Camera Raw compatibility list](https://helpx.adobe.com/camera-raw/desktop/get-started/overview-and-setup/camera-raw-compatible-applications.html) also names Photoshop Elements and Premiere Elements, and raw-host support does not establish use of this exact Camera Raw 2 directory.

## Producer activity boundary

`AppActivity.check(producers:)` returns `running`, `notObservedRunning`, or `unknown`. It reads current-user kernel process identity and executable-path metadata under the no-materialization policy. It does not read project contents, arguments, environment, or preferences, and does not close applications. Unsupported producers, incomplete observations, malformed buffers, and detected process churn yield unknown activity.

Compiled names cover selected Photoshop, After Effects, render-engine, Lightroom Classic, Media Encoder, and Resolve identities. Bridge has no independently verified executable identity: the trusted metadata lookup was refused by ownership checks, while an application inventory label alone does not verify its executable or helpers. The candidate's declared producer set therefore yields `unknown`. Executable and bundle-family matching is conservative but incomplete; independently renamed helpers outside recognized bundles can escape it.

For a rule with `requiresClosedApplications: true`, the scanner checks activity before and after each path job and discards that job's findings if either observation is running or unknown. Findings retain the required producer set. Grouping and removal reject a mismatch against the current rule, including an attempt to remove the requirement. Removal repeats activity checks before staging, capture, and Trash. These are observations, not locks: another process can launch or exec after a check. Existing [removal and recovery limits](REMOVAL.md) still apply.

## Grouping and attribution

`CreativeCacheGrouping.group` consumes a completed report, its rule set, reviewed mappings, and optional caller-supplied project resolutions. It performs no filesystem lookup or path-based project inference. A mapping must retain every rule producer. Application-wide groups remain separate from project caches with unknown attribution; absent or ambiguous association evidence stays unknown.

An explicit project association retains its supplied provenance per finding. It is not a claim that RACKET discovered or verified vendor metadata. Group identity combines the full producer set declared by the rule with a stable project ID; matching project titles do not merge identities. Paths and opaque project IDs use exact UTF-8 identity, including when Swift considers two Unicode spellings equivalent. Retaining all declared producers does not establish that the provisional vendor catalogue is complete.

Groups preserve the original findings, internal observations, producer requirements, risk, reasons, and regeneration costs. They reject duplicate or inconsistent inputs, version mismatches, invalid timestamps, and size overflow. Totals report observed allocated bytes. `lastTouched` is the latest finding modification time, not a verified project-open or application-activity timestamp. Mixed-risk and judgement groups cannot become preselectable through attribution; grouping never grants removal permission.

## Remaining verification

- Complete the candidate's producer and helper catalogue, including Bridge, and validate supported vendor versions and process races.
- Verify candidate contents, regeneration, and recovery with controlled vendor fixtures before enabling any rule. Preserve original media, preferences, projects, generated user content, and mixed support directories.
- Establish a reliable, read-only vendor metadata resolver before claiming automatic project attribution; ambiguous and unsupported formats must remain unattributed.
- Complete vendor verification and present the checkpoint for owner review. The current CI result validates the implemented synthetic boundaries and ordinary-file integration, not vendor cache safety, complete producer coverage, or automatic project attribution.
